# 제품 요구사항 정의서 (PRD): Universal Alarm System (가칭: OmniAlarm)

## 1. 개요 (Overview)
본 문서는 목장, 공장, 집, 사무실 등 다양한 환경에서 발생하는 주요 상황(전기, 인터넷, 화재, 가스 등)을 실시간으로 감지하고 알람을 제공하는 MSA(Microservices Architecture) 기반 시스템의 요구사항을 정의합니다.
이 시스템은 모달(Modal) 형태의 프론트엔드 위젯으로 개발되어 어떠한 외부 애플리케이션(웹/앱)에도 쉽게 연동 및 부착할 수 있도록 설계됩니다.

---

## 2. 제품 비전 및 목표 (Vision & Goals)
* **범용성**: 특정 장소에 국한되지 않고 모든 공간의 상태를 모니터링.
* **확장성**: 초기(전기/인터넷) 모니터링을 시작으로 가스, 화재, 가전제품(세탁기, 건조기, TV) 등으로 감지 대상을 무한히 확장할 수 있는 플러그인 형태의 유연한 아키텍처.
* **이식성**: 타 서비스에 쉽게 통합할 수 있는 플러그앤플레이(Plug & Play) 방식의 모달 UI 제공 (Web Component 방식 우선 채택).

---

## 3. 핵심 기능 (MVP - Phase 1)

### 3.1. 임베더블 알람 모달 UI
* **기능**: 외부 서비스(예: 목장 관리 앱, 스마트홈 앱)에 삽입 가능한 모달 UI.
* **삽입 방식**: **Web Component** (`<omni-alarm-widget>`) 방식 채택.
  * `<script>` 태그 한 줄로 연동 가능.
  * Shadow DOM으로 호스트 앱의 CSS 스타일과 충돌 없음.
  * iframe 방식 대비 부모 앱과 이벤트 통신 용이.
* **요구사항**:
  * 현재 상태 요약(정상/경고/위험) 대시보드 표시.
  * 알람 발생 시 실시간 모달 팝업 및 알림 리스트 제공.
  * 알람 클릭 시 상세 정보(발생 시각, 상태, 운영자 입력 사유) 표시.
  * 허용 도메인(Origin) 기반 접근 제어 지원.

### 3.2. 인터넷 상태 모니터링 (Internet Status)
* **감지 대상**: 현장 인터넷 연결 온/오프 상태.
* **사유**: 시스템이 자동 추론 불가. 운영자가 사후 수동 입력.
* **구현 방안 (Heartbeat 패턴)**:
  * 현장 에이전트(3.4 참조)가 클라우드 서버로 **1분 간격 Heartbeat** 전송.
  * 서버가 **3분 이상 신호 미수신** 시 '인터넷 연결 끊김' 알람 발생.
  * 신호 재개 시 '인터넷 복구' 알람 및 단절 지속 시간 기록.
* **참고**: 구글 원격 데스크톱의 오프라인 전환은 보조 확인 수단으로 활용 가능하나, 시스템 공식 감지는 Heartbeat 패턴으로 처리.

### 3.3. 전기 온/오프 모니터링 (Power Status)
* **감지 대상**: 현장 주 전원 공급 상태.
* **핵심 제약**: 정전 시 감지 기기 자체도 꺼짐. 따라서 **배터리 백업이 있는 기기만 즉시 알람 가능**.
* **사유**: 시스템 자동 추론 불가(정전인지 차단기 트립인지 구분 불가). 운영자가 사후 수동 입력.

#### 구현 방안 비교

  | 방법 | 비용 | 신뢰도 | 즉시 알람 | 설명 |
  |---|---|---|---|---|
  | **UPS + Heartbeat 에이전트** | 중간 (UPS 5~15만원) | 높음 | 가능 | 에이전트와 공유기를 UPS에 연결. 정전 시 UPS 배터리로 수분~수십분 유지하며 서버에 즉시 알람. |
  | **LTE 내장 정전감지기** | 낮음 (월 통신비) | 매우 높음 | 가능 | 자체 유심(SIM) 내장 기기를 콘센트에 연결. 와이파이 없이 LTE로 독립 알람. 가장 확실한 방법. |
  | **클라우드 추론** | 0원 | 낮음 | 불가 (3분 지연) | Heartbeat 미수신 시 정전으로 추론. 인터넷 단절과 구분 불가. 보조 수단으로만 활용. |

#### Phase 1 권장 구성: UPS + Heartbeat 에이전트

```
[공유기] ──────────┐
                    ├── [소형 UPS (500VA, 5~10만원)]
[라즈베리파이] ────┘         │
  (에이전트 실행)            └── 배터리로 10~30분 유지
       │
       ├── 평상시: 1분마다 Heartbeat 전송 (인터넷 정상 신호)
       │
       └── 정전 발생 시:
             1. UPS가 USB로 에이전트에 "배터리 모드 전환" 이벤트 전송
             2. 에이전트가 즉시 서버에 "정전 발생" 알람 전송
             3. UPS 배터리 소진 전까지 알람 재전송 시도
             4. 전기 복구 시: UPS "AC 복구" 이벤트 → "전기 복구" 알람 전송
```

#### UPS 소프트웨어 연동 방법

* **Linux (라즈베리파이 권장)**:
  * NUT (Network UPS Tools) 설치: `apt install nut`
  * UPS를 USB로 연결하면 `/dev/usb/hiddev0` 등으로 인식
  * `upsc` 명령으로 UPS 상태 조회 (배터리 잔량, AC 상태, 부하율 등)
  * `upsmon` 데몬이 상태 변화 감지 → 에이전트에 이벤트 전달
  * 지원 UPS 제조사: APC, Eaton, CyberPower, Powercom 등 대부분 지원

* **Windows (기존 PC 활용 시)**:
  * 제조사 전용 소프트웨어 사용 (APC PowerChute, CyberPower PowerPanel 등)
  * 또는 NUT Windows 버전 설치
  * WMI 이벤트 구독으로 배터리 모드 전환 감지 가능

#### UPS 선택 기준

  | 항목 | 권장 사양 |
  |---|---|
  | 용량 | 500VA ~ 1000VA (공유기 + 라즈베리파이 기준 500VA 충분) |
  | 인터페이스 | USB 필수 (NUT 연동용) |
  | 예상 백업 시간 | 500VA 기준 공유기+라즈베리파이 약 30~60분 |
  | 추천 제품군 | APC Back-UPS 650, CyberPower CP1000PFCLCD 등 |

#### 에이전트가 서버로 전송하는 전력 이벤트 데이터

```json
{
  "device_id": "farm-agent-001",
  "event_type": "power_status_change",
  "status": "on_battery",
  "timestamp": "2026-09-20T10:30:00Z",
  "ups_info": {
    "battery_charge": 100,
    "battery_runtime_seconds": 1800,
    "input_voltage": 0,
    "load_percent": 12
  }
}
```

* `status` 값: `on_battery` (정전), `online` (전기 복구), `low_battery` (배터리 부족 경고)

#### LTE 정전감지기 병행 (예산 여건 시 강력 권장)

```
[LTE 정전감지기] ── 콘센트에 상시 연결
       │
       └── 전기 차단 시: 와이파이/공유기 없이 LTE로 직접 OmniAlarm 서버 호출
             → 공유기까지 꺼지는 최악의 상황도 커버
```
* UPS + LTE 감지기 병행 시: UPS 배터리 방전 후에도 LTE 감지기가 정전 지속을 알림.

### 3.4. 현장 에이전트 (On-site Agent)
* **역할**: 현장에 상시 설치되어 Heartbeat 전송, 전기/인터넷 상태 감지, 센서 데이터 수집을 담당하는 경량 프로그램.
* **지원 기기**:
  * 1순위: 라즈베리파이 또는 소형 미니PC (저전력, 상시 가동)
  * 2순위: 현장 기존 Windows/Linux PC
* **지원 OS**: Linux (Raspbian, Ubuntu), Windows 10+
* **통신 프로토콜**: HTTPS REST (Heartbeat), MQTT (센서 이벤트, 향후 확장)
* **인증 방식**: 기기별 고유 API Key (서버에서 발급 및 관리)
* **배포 방식**: Docker 컨테이너 또는 systemd 서비스 (Linux), Windows 서비스 (Windows)
* **UPS 연동**: USB HID 프로토콜 또는 NUT(Network UPS Tools)를 통해 UPS 배터리 모드 전환 이벤트 수신.

### 3.5. 환경 센서 모니터링 (Environment Sensor)
* **감지 대상**: 현장의 온도, 습도, CO2, 조도, 토양 수분 등 물리적 환경 지표.
* **주요 활용 장소**: 목장(축사 온습도), 공장(작업 환경), 비닐하우스(온실 관리), 서버실(온도 경보) 등.

#### 지원 센서 종류

  | 센서 | 측정 항목 | 활용 예 |
  |---|---|---|
  | DHT22 / SHT31 | 온도, 습도 | 축사, 온실, 창고 환경 관리 |
  | MH-Z19 / SCD40 | CO2 농도 | 축사 환기 필요 여부 감지 |
  | BH1750 | 조도(밝기) | 일조량 모니터링 |
  | 토양 수분 센서 | 토양 습도 | 농경지 관개 필요 여부 |
  | DS18B20 | 온도 (방수형) | 수조, 외부 기온 |

#### 연동 방식
* 센서를 현장 에이전트(라즈베리파이 GPIO 또는 USB)에 직접 연결.
* 에이전트가 **설정 주기(기본 5분)마다** 측정값을 서버로 전송.
* MQTT 프로토콜 사용 (저전력, 실시간 스트리밍에 적합).

#### 알람 조건
* 관리자가 임계값(Threshold)을 직접 설정:

  | 예시 조건 | 알람 |
  |---|---|
  | 온도 > 35°C | `[축사] 고온 경보 - 현재 37.2°C` |
  | 습도 > 85% | `[창고] 과습 경보 - 현재 88%` |
  | CO2 > 2000ppm | `[축사] CO2 농도 위험 - 즉시 환기 필요` |
  | 온도 < 0°C | `[수도관] 동파 위험 - 현재 -1.5°C` |

* 임계값 초과 → 경보 / 임계값 복귀 → 복구 알람 발송.
* 측정값 이력은 Event/History Service에 저장하여 그래프 조회 가능.

---

### 3.6. 날씨 모니터링 (Weather Monitor)
* **목적**: 현장 위치 기반 외부 날씨 정보를 실시간으로 수신하여, 기상 특보 및 날씨 변화에 따른 사전 알람 제공.
* **데이터 소스**:
  * 기상청 오픈 API (단기예보, 특보 정보) — 국내 무료
  * OpenWeatherMap API — 글로벌, 분 단위 갱신 유료 플랜

#### 제공 기능

  | 기능 | 설명 |
  |---|---|
  | 현재 날씨 표시 | 현장 위치 기반 온도, 날씨 상태, 강수량 |
  | 일기예보 | 오늘/내일 날씨 예보 (6시간 단위) |
  | 기상 특보 알람 | 강풍, 폭우, 한파, 폭설 특보 발령 즉시 알람 |
  | 작업 적합성 알람 | 설정 조건(예: 풍속 > 10m/s) 충족 시 "야외 작업 주의" 알람 |

#### 알람 조건 예시

  | 조건 | 알람 |
  |---|---|
  | 기상청 특보 발령 (강풍, 한파 등) | `[날씨] 강풍 특보 - 풍속 예상 15m/s` |
  | 강수 확률 > 70% (6시간 이내) | `[날씨] 비 예보 - 6시간 내 강수 확률 80%` |
  | 기온 < -5°C 예보 | `[날씨] 한파 예보 - 내일 최저 -7°C, 동파 주의` |
  | 폭염 (기온 > 33°C 예보) | `[날씨] 폭염 예보 - 가축 열사병 주의` |

#### 구현 방식
* Endpoint Health Monitor와 유사하게, 클라우드 서버가 기상 API를 주기적으로 호출 (현장 에이전트 불필요).
* 현장별로 위도/경도 또는 행정구역 코드 등록.
* 기상 특보는 API 폴링 주기 5분, 일반 날씨 갱신 주기 30분.

---

### 3.7. 행정 연락처 관리 (Administrative Contact)
* **목적**: 알람 발생 시 담당자에게 즉시 연락하거나, 관련 행정기관·업체 연락처를 한 곳에서 관리하여 빠른 대응 지원.

#### 연락처 유형

  | 유형 | 예시 |
  |---|---|
  | 내부 담당자 | 목장주, 관리인, 야간 당직자 |
  | 긴급 기관 | 소방서(119), 경찰(112), 한전 고장신고(123) |
  | 유지보수 업체 | 전기공사업체, 인터넷 ISP 고객센터, 설비업체 |
  | 지역 행정기관 | 읍사무소, 농업기술센터, 가축방역 담당 |

#### 기능 요구사항
* 연락처 등록/수정/삭제 (이름, 전화번호, 역할, 비고).
* **알람 수신자 지정**: 알람 유형별로 수신 담당자 매핑.
  * 예: 정전 알람 → 전기업체 + 목장주 / 가스 알람 → 소방서 + 목장주
* **에스컬레이션**: 1차 담당자 미확인 시 N분 후 2차 담당자에게 자동 재전송.
* **연락처 카드 UI**: 모달 위젯 내에서 긴급 연락처 빠른 조회 가능 (전화 연결 버튼 포함).
* **알람 대응 이력**: 알람 수신 → 담당자 확인(ACK) → 처리 완료까지 상태 추적.

#### 알람-담당자 매핑 예시

  | 알람 유형 | 1차 수신자 | 2차 수신자 (미확인 10분 후) |
  |---|---|---|
  | 정전 | 목장주 | 전기공사업체 |
  | 인터넷 단절 | 목장주 | ISP 고객센터 |
  | 고온 경보 (축사) | 관리인 | 목장주 |
  | 기상 특보 | 목장주 | 관리인 |
  | 화재 감지 | 소방서(119) | 목장주 |

---

### 3.8. 장비별 URL 접근 모니터링 (Endpoint Health Monitor)
* **배경**: 네답(NedaP), IP 카메라, NAS 등 웹 인터페이스를 가진 장비는 인터넷이 끊기거나 장비가 다운되면 접속 URL이 HTTP 오류(502, 503, 504 등)를 반환하거나 응답 자체가 없어짐. 이를 감지하여 알람 발생.
* **감지 대상**: 관리자가 등록한 임의의 URL (예: `https://cnuniv.vpu-online.com/`)
* **동작 방식**:
  * 클라우드 서버가 등록된 URL을 **주기적으로 폴링** (기본 1분 간격, 설정 변경 가능).
  * 현장 에이전트 불필요 — 서버가 외부에서 직접 URL을 호출.
* **알람 조건**:

  | 조건 | 상태 | 알람 내용 |
  |---|---|---|
  | HTTP 4xx / 5xx 응답 (예: 502 Bad Gateway) | 오프라인/오류 | `[장비명] 접속 오류 - HTTP 502` |
  | 응답 타임아웃 (기본 10초) | 응답 없음 | `[장비명] 응답 없음 - 타임아웃` |
  | 특정 오류 문자열 포함 (예: "device appears to be offline") | 오프라인 | `[장비명] 장비 오프라인 감지` |
  | 정상 복구 (200 OK 재개) | 복구 | `[장비명] 접속 복구됨` |

* **장비 등록 정보**:
  * 장비명 (예: 네답, IP카메라 1번)
  * 모니터링 URL
  * 기대 HTTP 상태 코드 (기본: 200)
  * 오류로 간주할 응답 문자열 패턴 (선택)
  * 폴링 간격 (기본: 1분)
  * 알람 수신자

* **현장 에이전트 vs URL 폴링 비교**:

  | 항목 | Heartbeat (3.2) | URL 폴링 (3.5) |
  |---|---|---|
  | 에이전트 설치 필요 | 필요 | 불필요 |
  | 인터넷 단절 감지 | 가능 | 가능 |
  | 장비별 개별 감지 | 불가 | 가능 |
  | 전기 단절 감지 | 가능 (UPS와 함께) | 불가 |
  | 적합 대상 | 현장 전체 네트워크 | 웹 인터페이스 보유 장비 |

* **추론 로직**: Heartbeat와 URL 폴링을 결합하면 사유를 추론 가능.
  * Heartbeat 정상 + URL 502 → 해당 **장비 자체 문제** (전원, 펌웨어 등)
  * Heartbeat 끊김 + URL 502 → **현장 인터넷 단절** 가능성
  * Heartbeat 끊김 + URL 타임아웃 → **현장 정전 또는 인터넷 단절**

---

## 4. 비기능 요구사항 (Non-Functional Requirements)

### 4.1. 보안
* 현장 에이전트 ↔ 서버 통신: HTTPS + 기기별 API Key 인증.
* 모달 위젯 삽입: 허용 Origin 화이트리스트 관리 (CORS 제어).
* API Key 유출 시 즉시 폐기 및 재발급 기능 제공.

### 4.2. 신뢰성
* Heartbeat 누락 판단 기준: 3분 (설정 변경 가능).
* 알람 중복 발송 방지: 동일 이벤트에 대해 1회만 알람 발송 (상태 전환 기준).
* 서버 장애 시 에이전트가 로컬에 이벤트를 버퍼링 후 재전송.

### 4.3. 확장성
* 새로운 센서/기기 추가 시 에이전트 코드 수정 없이 플러그인 형태로 등록 가능.
* 마이크로서비스별 독립 배포 및 스케일아웃 지원.

---

## 5. 아키텍처 (High-Level MSA Architecture)

### 5.1. 백엔드 서비스

```
                         ┌─────────────────────────────────┐
                         │         외부 데이터 소스          │
                         │  [기상청/OpenWeather API]         │
                         │  [URL 폴링 (장비 상태)]           │
                         └────────────┬────────────────────┘
                                      │
[현장 에이전트] ──→ [Gateway Service] ←┘
  (Heartbeat,              │
   센서 데이터,   ┌─────────┼──────────┬──────────────┐
   UPS 이벤트)   ▼         ▼          ▼              ▼
         [Device/Agent  [Rule      [Event/       [Contact
           Service]     Engine     History       Service]
         (기기 등록,    Service]   Service]     (연락처,
          Heartbeat)   (알람 조건  (이력,        에스컬레이션,
                        판단 및    통계,         ACK 추적)
                        사유 추론) 센서 그래프)
                            │
                            ▼
                   [Notification Service]
                   (카카오톡, SMS, 이메일,
                    앱 푸시, 웹소켓)
```

* **Gateway Service**: 모든 트래픽 진입점. 인증, 라우팅, CORS 제어.
* **Device/Agent Service**: 현장 기기 등록, API Key 관리, Heartbeat 수집 및 상태 유지. 센서 데이터 수신.
* **Endpoint Health Monitor Service**: 등록된 URL 주기적 폴링. HTTP 상태 코드 및 응답 내용 감지. 클라우드에서 독립 실행.
* **Weather Monitor Service**: 기상청/OpenWeather API 주기적 호출. 특보 및 예보 기반 알람 생성. 클라우드에서 독립 실행.
* **Rule Engine Service**: Heartbeat + URL 폴링 + 센서값 + 날씨 결과를 교차 분석하여 알람 조건 평가 및 사유 추론.
* **Event/History Service**: 모든 알람 이벤트 및 센서 측정값 저장. 이력 조회, 그래프, 통계 제공.
* **Contact Service**: 연락처 관리, 알람-담당자 매핑, 에스컬레이션 규칙, ACK 상태 추적.
* **Notification Service**: 알람을 지정 채널(카카오톡, SMS, 이메일, 웹소켓)로 전달.

### 5.2. 프론트엔드

* **Widget Client**: Vanilla JS 기반 Web Component (`<omni-alarm-widget>`). 단일 번들(`omni-alarm.js`)로 배포. 어떤 앱에서도 `<script>` 태그 한 줄로 삽입 가능.

---

## 6. 알람 채널 결정 (Phase 1 포함 범위)

| 채널 | Phase 1 포함 | 비고 |
|---|---|---|
| 모달 UI (웹소켓 실시간) | 포함 | 위젯 내 실시간 알람 표시 |
| 카카오톡 알림톡 | 포함 | 비용 효율 높음, 한국 사용자 친화적 |
| SMS | 미포함 (Phase 2) | 카카오톡으로 대체 |
| 이메일 | 미포함 (Phase 2) | 실시간성 낮음 |
| 앱 푸시 | 미포함 (Phase 3) | 모바일 앱 개발 후 연동 |

---

## 7. 향후 확장 계획 (Phase 2 & Beyond)

새로운 MSA 마이크로서비스를 추가하여 기능 확장:
* **가전/홈 IoT 연동**: 삼성 SmartThings, LG ThinQ, Tuya 등의 개방형 API를 연동하여 세탁기/건조기 완료 시간, TV 전원 상태 등 모니터링.
* **안전 센서**: 가스 누출 감지기, 스마트 화재(연기) 감지기 등과 연동.
* **추가 환경 센서**: 토양 수분, CO2, 풍속계, 우량계 등 확장.
* **알람 채널 확장**: SMS, 이메일, 앱 푸시 알림.
* **대시보드**: 장소별 상태 현황, 알람 이력 통계, 정전/단절 빈도 분석, 센서 측정값 그래프.
* **행정 연동 고도화**: 농업기술센터, 가축방역기관 자동 통보 연동. 알람 리포트 자동 생성 및 이메일 발송.

---

## 8. 미결 사항 (Open Questions)

1. **현장 기준 기기**: 목장 현장에 설치할 기기를 라즈베리파이로 확정할지, 기존 PC를 활용할지 결정 필요.
2. **UPS 구매 여부**: Phase 1에 UPS 도입을 포함할지, 우선 클라우드 추론 방식으로 시작할지 결정 필요.
